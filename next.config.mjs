/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['basic-ecommerce-server.onrender.com', 'localhost'],
  },
  reactStrictMode: false,
};

export default nextConfig;
